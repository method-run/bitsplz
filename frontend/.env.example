# API Configuration
# Browser needs to use localhost since it's not in Docker network
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000/ws

# Note: If you need to make container-to-container calls, use:
# VITE_API_URL=http://backend:3000
# VITE_WS_URL=ws://backend:3000/ws

# Environment
NODE_ENV=development
